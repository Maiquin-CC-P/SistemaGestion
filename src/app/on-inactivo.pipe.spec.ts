import { OnInactivoPipe } from './on-inactivo.pipe';

describe('OnInactivoPipe', () => {
  it('create an instance', () => {
    const pipe = new OnInactivoPipe();
    expect(pipe).toBeTruthy();
  });
});
